- FastCDC + SHA-256 CAS pipeline achieves 83.7% dedup on identical-file workloads
- Write-back LRU cache delivers 9.2x sequential write throughput over ext4
- Copy-on-write snapshots created in O(1) CAS operations via recipe-pointer cloning
- TagFS resolves composite tag-path intersections lazily over sled-backed indexes
- Mixed-workload disk use reduced 57.6% vs. ext4; zero integrity errors across all runs
